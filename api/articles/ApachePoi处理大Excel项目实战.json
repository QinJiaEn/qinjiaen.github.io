{"title":"大文件 Excel 解析内存飙升(OOM)排查与极限调优纪实","slug":"ApachePoi处理大Excel项目实战","date":"2026-01-15T04:00:00.000Z","updated":"2026-06-10T08:40:43.804Z","comments":true,"path":"api/articles/ApachePoi处理大Excel项目实战.json","excerpt":null,"covers":null,"content":"<h1 id=\"大文件-Excel-解析内存飙升-OOM-排查与极限调优纪实\"><a href=\"#大文件-Excel-解析内存飙升-OOM-排查与极限调优纪实\" class=\"headerlink\" title=\"大文件 Excel 解析内存飙升(OOM)排查与极限调优纪实\"></a>大文件 Excel 解析内存飙升(OOM)排查与极限调优纪实</h1><p><strong>业务场景</strong>：财税系统申报表与底稿计算（跨表公式引擎） </p>\n<p><strong>技术栈</strong>：Java 17, Apache POI, Spring Boot, G1GC ,JVM</p>\n<h2 id=\"1-背景与问题现象\"><a href=\"#1-背景与问题现象\" class=\"headerlink\" title=\"1. 背景与问题现象\"></a>1. 背景与问题现象</h2><p>在开发税务申报底稿计算模块时，业务需要同时将三个大型 Excel 文件（序时账、底稿、申报主表）加载到内存中，并通过 22 个 Service 进行复杂的跨表公式计算，最终将结果 <code>write</code> 写入磁盘生成物理文件。</p>\n<ul>\n<li><strong>现象</strong>：在单线程异步执行时，通过 JVisualVM 监控发现，堆内存（Used Heap）在计算期间会一路狂飙至 1.7G - 2.0G，引发了对多并发场景下 OOM（内存溢出）的严重担忧。</li>\n<li><strong>目标</strong>：查明内存飙升元凶，在保证业务（最终能成功导出完整 Excel）的前提下，将内存峰值压制到最低。</li>\n</ul>\n<h2 id=\"2-踩坑记录与排雷（避坑指南）\"><a href=\"#2-踩坑记录与排雷（避坑指南）\" class=\"headerlink\" title=\"2. 踩坑记录与排雷（避坑指南）\"></a>2. 踩坑记录与排雷（避坑指南）</h2><p>在探索极限降内存的过程中，经历了以下几个极具欺骗性的方向：</p>\n<h3 id=\"❌-坑-1：物理毁灭-DOM-节点（破坏文件结构）\"><a href=\"#❌-坑-1：物理毁灭-DOM-节点（破坏文件结构）\" class=\"headerlink\" title=\"❌ 坑 1：物理毁灭 DOM 节点（破坏文件结构）\"></a>❌ 坑 1：物理毁灭 DOM 节点（破坏文件结构）</h3><ul>\n<li><strong>思路</strong>：在 <code>for</code> 循环中计算完一行后，直接通过 <code>sheet.removeRow(row)</code> 或 <code>cellIterator.remove()</code> 销毁单元格节点。</li>\n<li><strong>后果</strong>：致命的业务 Bug。由于业务最后需要调用 <code>workbook.write(os)</code> 保存结果文件，中途销毁节点会导致最终导出的 Excel 变成只有部分数据的残缺表或空白表。在 DOM 模式（XSSFWorkbook）下，只要最后还要写盘，整个 XML 树就必须完整存活。</li>\n</ul>\n<h3 id=\"❌-坑-2：盲目听信-AI-的幻觉-API-与流式读取\"><a href=\"#❌-坑-2：盲目听信-AI-的幻觉-API-与流式读取\" class=\"headerlink\" title=\"❌ 坑 2：盲目听信 AI 的幻觉 API 与流式读取\"></a>❌ 坑 2：盲目听信 AI 的幻觉 API 与流式读取</h3><ul>\n<li><strong>幻觉 API</strong>：某些资料建议调用 <code>CollaboratingWorkbooksEnvironment.clearAll()</code>。经查证，POI 源码根本不存在此方法。</li>\n<li><strong>张冠李戴的 SXSSF</strong>：被建议使用 <code>SXSSF</code> 解决大文件 OOM。事实上，<code>SXSSF</code> 仅适用于<strong>单向流式写入</strong>，完全不支持我们业务中极其依赖的随机读取和公式计算（Formula Evaluator）。</li>\n</ul>\n<h3 id=\"❌-坑-3：JVM-乱下“紧箍咒”（IHOP-死亡循环）\"><a href=\"#❌-坑-3：JVM-乱下“紧箍咒”（IHOP-死亡循环）\" class=\"headerlink\" title=\"❌ 坑 3：JVM 乱下“紧箍咒”（IHOP 死亡循环）\"></a>❌ 坑 3：JVM 乱下“紧箍咒”（IHOP 死亡循环）</h3><ul>\n<li><strong>思路</strong>：试图通过 <code>-XX:InitiatingHeapOccupancyPercent=20</code> 强迫 G1GC 在 400M 时去扫地。</li>\n<li><strong>后果</strong>：由于存活对象的体积远大于 400M，导致 JVM 陷入疯狂的 Concurrent Marking（并发标记）死循环。CPU 飙升至 100%，居委会大妈只贴标签不扔垃圾，导致程序假死。</li>\n</ul>\n<h2 id=\"3-终极探秘：剥洋葱式的内存剖析\"><a href=\"#3-终极探秘：剥洋葱式的内存剖析\" class=\"headerlink\" title=\"3. 终极探秘：剥洋葱式的内存剖析\"></a>3. 终极探秘：剥洋葱式的内存剖析</h2><p>为了找到内存真相，我们剥离了所有 Spring 业务代码，写了一个纯粹的**基线测试类 (MemoryBaselineTest)**，得到了底层真相：</p>\n<ol>\n<li><strong>绝对的物理铁底（654.45 MB）</strong>： 通过强制 <code>System.gc()</code> 后测得，三大工作簿加载成 DOM 树后，一滴水分不掺的物理体积就是 654MB。配合 Spring 框架本身的开销，<strong>1.2G 的基础存活内存是无法逾越的物理法则。</strong> <em>(注：得益于前置开发的 <code>ShadowWorkbookUtil</code> 影子表过滤机制，否则原生几十万行的序时账足以将这 654MB 撑爆至数 GB。)</em></li>\n<li><strong>飙升的波峰（临时 XML 垃圾）</strong>： 从 1.2G 冲到 1.8G 的那 600M~800M 空间，并非内存泄漏，而是 POI 底层在执行 <code>getCellValue</code> 和解析公式时，瞬间产生的海量短命 XML 解析碎屑（String, Cursor 等）。</li>\n</ol>\n<h2 id=\"4-破局之道：黄金杀招与最佳实践\"><a href=\"#4-破局之道：黄金杀招与最佳实践\" class=\"headerlink\" title=\"4. 破局之道：黄金杀招与最佳实践\"></a>4. 破局之道：黄金杀招与最佳实践</h2><h3 id=\"优化一：代码级“防内存泄漏”杀招\"><a href=\"#优化一：代码级“防内存泄漏”杀招\" class=\"headerlink\" title=\"优化一：代码级“防内存泄漏”杀招\"></a>优化一：代码级“防内存泄漏”杀招</h3><p>在 22 个 Service 的循环遍历中，公式引擎（Evaluator）会缓存大量的 AST 语法树。这是唯一会演变成长期内存泄漏的元凶。 <strong>解决方案</strong>：在计算循环内部，高频调用清空缓存方法，逼迫引擎吐出中间结果。</p>\n<p>Java</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">for (int i = 5; i &lt; 31; i++) &#123;</span><br><span class=\"line\">    // ... 执行业务计算，读取单元格 ...</span><br><span class=\"line\">    </span><br><span class=\"line\">    // 【黄金杀招】：斩断公式引擎缓存，防内存泄漏，释放空间给 XML 临时垃圾</span><br><span class=\"line\">    if (evaluator != null) &#123;</span><br><span class=\"line\">        evaluator.clearAllCachedResultValues();</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h3 id=\"优化二：JVM-G1GC-顺水推舟调优\"><a href=\"#优化二：JVM-G1GC-顺水推舟调优\" class=\"headerlink\" title=\"优化二：JVM G1GC 顺水推舟调优\"></a>优化二：JVM G1GC 顺水推舟调优</h3><ul>\n<li><strong>在 2G 内存受限环境的压测配置</strong>： 不瞎配置 IHOP，让 G1GC 自动发挥。1.2G 进老年代，剩下 800M 给年轻代装临时垃圾，满了自动 Minor GC 清理。 <code>java -Xms2G -Xmx2G -XX:+UseG1GC &quot;-Dfile.encoding=UTF-8&quot; -jar tax-admin.jar</code></li>\n<li><strong>满足目标的jvm参数</strong>: <code>java -Xms2G -Xmx2G -XX:+UseG1GC -XX:+UnlockExperimentalVMOptions -XX:G1MaxNewSizePercent=15 -XX:MaxGCPauseMillis=50 &quot;-Dfile.encoding=UTF-8&quot; -jar tax-admin.jar</code></li>\n</ul>\n","more":"<h1 id=\"大文件-Excel-解析内存飙升-OOM-排查与极限调优纪实\"><a href=\"#大文件-Excel-解析内存飙升-OOM-排查与极限调优纪实\" class=\"headerlink\" title=\"大文件 Excel 解析内存飙升(OOM)排查与极限调优纪实\"></a>大文件 Excel 解析内存飙升(OOM)排查与极限调优纪实</h1><p><strong>业务场景</strong>：财税系统申报表与底稿计算（跨表公式引擎） </p>\n<p><strong>技术栈</strong>：Java 17, Apache POI, Spring Boot, G1GC ,JVM</p>\n<h2 id=\"1-背景与问题现象\"><a href=\"#1-背景与问题现象\" class=\"headerlink\" title=\"1. 背景与问题现象\"></a>1. 背景与问题现象</h2><p>在开发税务申报底稿计算模块时，业务需要同时将三个大型 Excel 文件（序时账、底稿、申报主表）加载到内存中，并通过 22 个 Service 进行复杂的跨表公式计算，最终将结果 <code>write</code> 写入磁盘生成物理文件。</p>\n<ul>\n<li><strong>现象</strong>：在单线程异步执行时，通过 JVisualVM 监控发现，堆内存（Used Heap）在计算期间会一路狂飙至 1.7G - 2.0G，引发了对多并发场景下 OOM（内存溢出）的严重担忧。</li>\n<li><strong>目标</strong>：查明内存飙升元凶，在保证业务（最终能成功导出完整 Excel）的前提下，将内存峰值压制到最低。</li>\n</ul>\n<h2 id=\"2-踩坑记录与排雷（避坑指南）\"><a href=\"#2-踩坑记录与排雷（避坑指南）\" class=\"headerlink\" title=\"2. 踩坑记录与排雷（避坑指南）\"></a>2. 踩坑记录与排雷（避坑指南）</h2><p>在探索极限降内存的过程中，经历了以下几个极具欺骗性的方向：</p>\n<h3 id=\"❌-坑-1：物理毁灭-DOM-节点（破坏文件结构）\"><a href=\"#❌-坑-1：物理毁灭-DOM-节点（破坏文件结构）\" class=\"headerlink\" title=\"❌ 坑 1：物理毁灭 DOM 节点（破坏文件结构）\"></a>❌ 坑 1：物理毁灭 DOM 节点（破坏文件结构）</h3><ul>\n<li><strong>思路</strong>：在 <code>for</code> 循环中计算完一行后，直接通过 <code>sheet.removeRow(row)</code> 或 <code>cellIterator.remove()</code> 销毁单元格节点。</li>\n<li><strong>后果</strong>：致命的业务 Bug。由于业务最后需要调用 <code>workbook.write(os)</code> 保存结果文件，中途销毁节点会导致最终导出的 Excel 变成只有部分数据的残缺表或空白表。在 DOM 模式（XSSFWorkbook）下，只要最后还要写盘，整个 XML 树就必须完整存活。</li>\n</ul>\n<h3 id=\"❌-坑-2：盲目听信-AI-的幻觉-API-与流式读取\"><a href=\"#❌-坑-2：盲目听信-AI-的幻觉-API-与流式读取\" class=\"headerlink\" title=\"❌ 坑 2：盲目听信 AI 的幻觉 API 与流式读取\"></a>❌ 坑 2：盲目听信 AI 的幻觉 API 与流式读取</h3><ul>\n<li><strong>幻觉 API</strong>：某些资料建议调用 <code>CollaboratingWorkbooksEnvironment.clearAll()</code>。经查证，POI 源码根本不存在此方法。</li>\n<li><strong>张冠李戴的 SXSSF</strong>：被建议使用 <code>SXSSF</code> 解决大文件 OOM。事实上，<code>SXSSF</code> 仅适用于<strong>单向流式写入</strong>，完全不支持我们业务中极其依赖的随机读取和公式计算（Formula Evaluator）。</li>\n</ul>\n<h3 id=\"❌-坑-3：JVM-乱下“紧箍咒”（IHOP-死亡循环）\"><a href=\"#❌-坑-3：JVM-乱下“紧箍咒”（IHOP-死亡循环）\" class=\"headerlink\" title=\"❌ 坑 3：JVM 乱下“紧箍咒”（IHOP 死亡循环）\"></a>❌ 坑 3：JVM 乱下“紧箍咒”（IHOP 死亡循环）</h3><ul>\n<li><strong>思路</strong>：试图通过 <code>-XX:InitiatingHeapOccupancyPercent=20</code> 强迫 G1GC 在 400M 时去扫地。</li>\n<li><strong>后果</strong>：由于存活对象的体积远大于 400M，导致 JVM 陷入疯狂的 Concurrent Marking（并发标记）死循环。CPU 飙升至 100%，居委会大妈只贴标签不扔垃圾，导致程序假死。</li>\n</ul>\n<h2 id=\"3-终极探秘：剥洋葱式的内存剖析\"><a href=\"#3-终极探秘：剥洋葱式的内存剖析\" class=\"headerlink\" title=\"3. 终极探秘：剥洋葱式的内存剖析\"></a>3. 终极探秘：剥洋葱式的内存剖析</h2><p>为了找到内存真相，我们剥离了所有 Spring 业务代码，写了一个纯粹的**基线测试类 (MemoryBaselineTest)**，得到了底层真相：</p>\n<ol>\n<li><strong>绝对的物理铁底（654.45 MB）</strong>： 通过强制 <code>System.gc()</code> 后测得，三大工作簿加载成 DOM 树后，一滴水分不掺的物理体积就是 654MB。配合 Spring 框架本身的开销，<strong>1.2G 的基础存活内存是无法逾越的物理法则。</strong> <em>(注：得益于前置开发的 <code>ShadowWorkbookUtil</code> 影子表过滤机制，否则原生几十万行的序时账足以将这 654MB 撑爆至数 GB。)</em></li>\n<li><strong>飙升的波峰（临时 XML 垃圾）</strong>： 从 1.2G 冲到 1.8G 的那 600M~800M 空间，并非内存泄漏，而是 POI 底层在执行 <code>getCellValue</code> 和解析公式时，瞬间产生的海量短命 XML 解析碎屑（String, Cursor 等）。</li>\n</ol>\n<h2 id=\"4-破局之道：黄金杀招与最佳实践\"><a href=\"#4-破局之道：黄金杀招与最佳实践\" class=\"headerlink\" title=\"4. 破局之道：黄金杀招与最佳实践\"></a>4. 破局之道：黄金杀招与最佳实践</h2><h3 id=\"优化一：代码级“防内存泄漏”杀招\"><a href=\"#优化一：代码级“防内存泄漏”杀招\" class=\"headerlink\" title=\"优化一：代码级“防内存泄漏”杀招\"></a>优化一：代码级“防内存泄漏”杀招</h3><p>在 22 个 Service 的循环遍历中，公式引擎（Evaluator）会缓存大量的 AST 语法树。这是唯一会演变成长期内存泄漏的元凶。 <strong>解决方案</strong>：在计算循环内部，高频调用清空缓存方法，逼迫引擎吐出中间结果。</p>\n<p>Java</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">for (int i = 5; i &lt; 31; i++) &#123;</span><br><span class=\"line\">    // ... 执行业务计算，读取单元格 ...</span><br><span class=\"line\">    </span><br><span class=\"line\">    // 【黄金杀招】：斩断公式引擎缓存，防内存泄漏，释放空间给 XML 临时垃圾</span><br><span class=\"line\">    if (evaluator != null) &#123;</span><br><span class=\"line\">        evaluator.clearAllCachedResultValues();</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h3 id=\"优化二：JVM-G1GC-顺水推舟调优\"><a href=\"#优化二：JVM-G1GC-顺水推舟调优\" class=\"headerlink\" title=\"优化二：JVM G1GC 顺水推舟调优\"></a>优化二：JVM G1GC 顺水推舟调优</h3><ul>\n<li><strong>在 2G 内存受限环境的压测配置</strong>： 不瞎配置 IHOP，让 G1GC 自动发挥。1.2G 进老年代，剩下 800M 给年轻代装临时垃圾，满了自动 Minor GC 清理。 <code>java -Xms2G -Xmx2G -XX:+UseG1GC &quot;-Dfile.encoding=UTF-8&quot; -jar tax-admin.jar</code></li>\n<li><strong>满足目标的jvm参数</strong>: <code>java -Xms2G -Xmx2G -XX:+UseG1GC -XX:+UnlockExperimentalVMOptions -XX:G1MaxNewSizePercent=15 -XX:MaxGCPauseMillis=50 &quot;-Dfile.encoding=UTF-8&quot; -jar tax-admin.jar</code></li>\n</ul>\n","categories":[{"name":"入职后(数据脱敏处理)","path":"api/categories/入职后(数据脱敏处理).json"}],"tags":[{"name":"ApachePoi, excel","path":"api/tags/ApachePoi, excel.json"}]}